using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    GameObject leftHand;        
    GameObject rightHand;        

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (this.leftHand == null) { this.leftHand = GameObject.Find('Left Interaction Hand Contact Bones'); }
        if (this.rightHand == null) { this.rightHand = GameObject.Find('Right Interaction Hand Contact Bones'); }
        if (this.leftHand.activeSelf) this.leftHand.SetActive(false);
        if (this.rightHand.activeSelf) this.leftHand.SetActive(false);
        */
    }
}
